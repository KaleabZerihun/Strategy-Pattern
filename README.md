To run this project run the following in the commad line after you clone the project:
  - cd strategy-pattern
  - python -m venv venv
  - venv\Scripts\activate
  - pip install django
  - python manage.py runserver
